import functions from "node-blox-sdk/functions/index.js";
import shield from "node-blox-sdk/shield/index.js";
import internals from "node-blox-sdk/internals/index.js";
import env from "node-blox-sdk/env/index.js";
import shared from "node-blox-sdk/shared/index.js";

const { run } = functions;
const { getUID, getUser } = shield;
const { initialize } = internals;
const { init: envInit } = env;
const { getShared } = shared;

// For named export
export {
  functions,
  run,
  shield,
  getUID,
  getUser,
  internals,
  initialize,
  env,
  envInit,
  shared,
  getShared,
};

// For whole sdk default export
export default { functions, shield, internals, env };
